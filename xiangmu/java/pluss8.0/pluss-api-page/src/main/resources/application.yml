server:
  port: 9200
  servlet:
    context-path: /api
spring:
  datasource:
    url: jdbc:mysql://8.130.212.214:3306/zhihuiyanglao?useUnicode=true&characterEncoding=utf-8&useSSL=false&zeroDateTimeBehavior=convertToNull
    username: root
    password: Ylkj@2018
    driver-class-name: com.mysql.jdbc.Driver
    hikari.read-only: false
    hikari.connection-timeout: 60000
    hikari.idle-timeout: 60000
    hikari.validation-timeout: 3000
    hikari.max-lifetime: 60000
    hikari.login-timeout: 5
    hikari.maximum-pool-size: 60
    hikari.minimum-idle: 10
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 2048MB
      max-request-size: 2048MB
    tomcat.max-swallow-size: 2048MB
mybatis-plus:
  global-config:
    db-config:
      id-type: auto #id自增
  mapper-locations: classpath:mybatis/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志
    map-underscore-to-camel-case: true #禁止将下划线转为驼峰

